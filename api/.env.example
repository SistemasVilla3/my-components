# Prisma usa PostgreSQL (Neon/Supabase/RDS). Reemplaza credenciales reales:
DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DB?sslmode=require"

# Ejemplo adicional (Supabase / conexión con límite):
# DATABASE_URL="postgresql://USER:PASSWORD@HOST:6543/DB?connection_limit=1&sslmode=require"
